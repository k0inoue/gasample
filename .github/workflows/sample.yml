# This is a basic workflow to help you get started with Actions

name: CI

on:
  # 指定ブランチで指定ファイルがPUSHされたときに実行する
  push:
  
  workflow_dispatch:
    inputs:
      result:
        description: 'Result'
        default: 0
        required: true

jobs:
  sample:
    runs-on: ubuntu-latest
    steps:
      - name: set-step
        id: step1
        run: echo "::set-output name=test::${{ github.event.inputs.result }}"
      - name: out-step
        run: |
          echo RESULT: ${{ steps.step1.outputs.test-result }}
